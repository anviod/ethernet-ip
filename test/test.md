以下测试用例为 Ethernet IP 模拟器的测试测试用例(需要全部通过)：
1. 设备发现
   - 使用 CIPDriver.discover() 能发现模拟器
   - 设备信息正确 (名称、厂商、类型) 现实正常

2. 连接建立
   - 能成功建立 TCP 连接到 127.0.0.1:44818
   - 能注册会话并获取 Session ID

3. CIP 通用消息
   - Get Attributes All 服务正常工作
   - 能获取设备标识信息

4. 标签读写
   - 单个标签读取正常
   - 单个标签写入正常
   - 批量读取正常
   - 批量写入正常

5. 数据类型支持
   - BOOL 类型正常
   - SINT/INT/DINT/LINT 类型正常
   - USINT/UINT/UDINT/ULINT 类型正常
   - REAL/LREAL 类型正常
   - STRING 类型正常

6. 协议合规性
   - 封装协议头格式正确
   - CIP 命令响应格式正确
   - 状态码返回正确

7. 错误处理
   - 无效标签返回正确错误码
   - 无效数据类型返回正确错误码
   - 连接断开后能正确处理